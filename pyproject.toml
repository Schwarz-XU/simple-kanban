[project]
name = "kanban-bob"
version = "0.1.0"
description = "Local Kanban board (Flask + SQLite)."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "flask==3.0.0",
]

[tool.uv]
# Keep it simple; lockfile will be generated by `uv lock`.

